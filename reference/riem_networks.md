# Get ASOS and AWOS networks

Get ASOS and AWOS networks

## Usage

``` r
riem_networks()
```

## Value

a data.frame (tibble tibble) with the names and codes of available
networks.

## Examples

``` r
if (FALSE) { # \dontrun{
riem_networks()
} # }
```
